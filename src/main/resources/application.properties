server.port:9876
#spring.mvc.view.prefix: /WEB-INF/jsp/
#spring.mvc.view.suffix: .jsp
app.welcome.message: Welcome to Spring Boot Application
app.welcome.title: Spring Boot Mustache Hello World Example
logging.level.org.springframework.web:ERROR
logging.level.com.mkyong:DEBUG

# Logging pattern for the console
logging.pattern.console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Logging pattern for file
logging.pattern.file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

logging.file:/springBootApp/logs/PeopleManagement.log
#By default, Spring Boot max file upload size is 1MB, you can configure the values via following application properties :
spring.http.multipart.max-file-size:10MB
spring.http.multipart.max-request-size:10MB

# create and drop tables and sequences, loads import.sql
spring.jpa.hibernate.ddl-auto:validate

# Oracle settings
#spring.datasource.url:jdbc:oracle:thin:@localhost:1521:xe
#spring.datasource.username:bishwa
#spring.datasource.password:bishwa
#spring.datasource.driver-class:oracle.jdbc.driver.OracleDriver
 
 # Database
db.driver: oracle.jdbc.driver.OracleDriver
db.url: jdbc:oracle:thin:@localhost:1521:xe
db.username: bishwa
db.password: bishwa
 
 
# Hibernate
hibernate.dialect: org.hibernate.dialect.OracleDialect
hibernate.show_sql: true
hibernate.hbm2ddl.auto: validate
entitymanager.packagesToScan: com.as


# HikariCP settings
# spring.datasource.hikari.*

#spring.datasource.hikari.connection-timeout:60000
#spring.datasource.hikari.maximum-pool-size:5